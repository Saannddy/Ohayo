services:
  web:
    build:
      context: .
      target: dev
    ports:
      - "1420:1420"
    volumes:
      - ./src:/app/src
      - ./index.html:/app/index.html
    environment:
      - NODE_ENV=development
